# Plan: agency home page only (FINAL. Supersedes plan_minimal_agency_site.md and plan_site_repositioning.md)

Owner directive 2026-08-27 (execute immediately, no approval gate): the overhaul is scrapped. Main is reset to the original site (d919a6a). The scrapped work lives on branch `overhaul-scrapped-2026-08-27` for reference/salvage. We change NOTHING on the site except the four items below.

## The four changes

1. MOVE HOME TO /finch, verbatim. app/page.tsx's current composition becomes app/finch/page.tsx. Content stays as it is today (the COO headline, the bird, everything) EXCEPT: (a) any link to /pricing repoints to /operations-audit, (b) any explicit price claim (R6,000, per location, everything included, the R2,000 audit fee) is minimally rewritten to per-scope framing ("priced per customer and scope, fixed after a free audit"), because those claims contradict item 3 and 4. Touch nothing else on the page. Remove the existing "/finch -> /" redirect in next.config.ts. /finch gets its own metadata (title "Finch by Vyso" idiom) and keeps/inherits the existing OG image treatment (bird allowed). Add /finch to sitemap.

2. NEW HOME PAGE at /: Vyso, the AI automation agency in Johannesburg for South African SMEs. Same design language as the rest of the site (.finch-site --fn-* tokens, grounds, gradients, grain, scroll idiom). STRONG SHORTCUT: the scrapped branch contains a finished, owner-directed agency home page (`git show overhaul-scrapped-2026-08-27:app/page.tsx`) built from exactly this positioning with the alive design the owner asked for (alternating paper/ink/blue grounds, integration wheel with Vyso hub, step rail, invoice scroll sequence, Roberto quote with [TNS_NUMBER] line, Finch and Orbit product cards, gradient close). Port it and its minimal component dependencies (e.g. the HomeHero/BriefPhone Vyso-relabel parameterisation, IntegrationsOrbit `hub` prop) onto the original tree, adapting links to the ORIGINAL site's surviving routes (industries pages still exist on the original site: the router/tiles may link to them; /how-we-work does NOT exist here, so any such link points to /operations-audit or an on-page section instead; no /pricing links). Home CTA: "Book your free audit" -> /operations-audit and/or "Talk to us" -> /contact. Home metadata: title "Vyso, AI automation agency in South Africa", description aligned; root layout default title updated the same way (the old default carries the COO line and must change); og default image: regenerate with the agency line, no bird.

3. REMOVE /pricing: delete app/pricing/**, 301 /pricing -> /operations-audit in next.config.ts, remove from sitemap, repoint every nav/footer/in-copy link to /pricing (original nav has a Pricing item: replace it with a Finch item so the moved page stays reachable; footer Pricing link likewise). Keep all other nav/footer items exactly as the original site has them. JSON-LD in app/layout.tsx pulls PRICE from components/finch/pricing/pricing-data.ts: keep the file if other pages import it, but remove monetary offers from JSON-LD (audit Service becomes free, price "0" ZAR; SoftwareApplication loses its price claim).

4. AUDIT PAGE /operations-audit: the audit is now FREE (was R2,000). Update every mention on the page (and its calculator/score subpages if they state the fee): free, about an hour with you, we find where money and time leak; output is a roadmap; pricing is per customer and per scope: every roadmap item gets a fixed build price and a monthly run price, quoted to you directly after the audit. Nothing is published. "Credited against" language is removed. Also update audit-fee mentions in obvious shared surfaces ONLY (faq data, layout JSON-LD, llms data if it states the fee). Do not do a site-wide scrub; this is not the overhaul.

## What does NOT change

Everything else: industries pages, learn, compare, resources, platform/modules, academy, orbit subsite, founding-client, free-scan, case studies, about, contact, faq (beyond audit-fee mentions), south-africa, integrations, login, app, robots. No banned-word sweep, no em-dash sweep, no footer restructure. The original site stays the original site.

## Copy rules for NEW copy only (home page, audit rewrites)

Sentence case, SA spelling, no em/en dashes, "R" never "ZAR" in prose, no rand amounts anywhere in new copy, no COO/module codenames in new copy, no invented client numbers ([TNS_NUMBER] placeholders where TnS figures are implied), "agency" is wanted.

## Execution

Single implementer, live repo (dev server on :3000 shows progress; restart it if the git reset broke it, it is run via the owner's preview tooling so prefer leaving it and letting HMR recover, or note if it needs a manual restart). ONE commit: "site: agency home page, move Finch to /finch, free audit, remove pricing" + trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Gate: npm run build && npm run lint (zero NEW errors; pre-existing product lint errors out of scope). Never stage untracked files (.ai/, free-scan, instrumentation-client.ts, lib/posthog-server.ts); stage by explicit path. Verify by screenshot in a NEW browser tab (never the owner's "seed" tab): home desktop + 375px, /finch spot check, audit page.
